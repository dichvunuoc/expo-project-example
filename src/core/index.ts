/**
 * App Layer (Public API)
 * FSD Layer: App (1)
 *
 * Contains application-wide configuration, providers, and styles.
 * The App layer is the entry point that composes all other layers.
 *
 * Dependencies: All layers (Pages, Widgets, Features, Entities, Shared)
 */

export * from './providers';
